import 'package:flutter/material.dart';

class VideoShareView extends StatelessWidget {
  const VideoShareView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
